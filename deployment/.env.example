# Common
REGISTRY_PROVIDER=dockerhub
IMAGE_TAG=replace-with-sha

# Docker Hub settings
DOCKERHUB_USERNAME=your-dockerhub-user
REGISTRY_NAMESPACE=your-dockerhub-user

# GHCR settings
GHCR_OWNER=your-github-user-or-org

# Runtime image prefix (auto-overridden by deploy script)
REGISTRY_PREFIX=your-dockerhub-user
